{
  "name": "@ai-di/alert-config",
  "version": "1.0.0",
  "description": "Shared alert type configuration used by backend-services and the Prometheus rule generator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
