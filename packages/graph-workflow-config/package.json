{
  "name": "@ai-di/graph-workflow-config",
  "version": "1.0.0",
  "description": "Shared graph workflow config types, config hash, and override helpers",
  "main": "dist/index.js",
  "types": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
