# Image Service

## Development Instructions

1. Create the `uv` virtual environment with `uv venv`
1. Install dependencies with `uv sync`
1. Start the server with `uv run main.py`

Note: You may need to set your Python interpreter to the one located in `.venv/bin/python` for import statements to resolve correctly in VS Code.
