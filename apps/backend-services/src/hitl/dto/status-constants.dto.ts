export enum DocumentStatusFilter {
  EXTRACTED = "extracted",
  ALL = "all",
}

export enum ReviewStatusFilter {
  PENDING = "pending",
  CLAIMED = "claimed",
  REVIEWED = "reviewed",
  FLAGGED = "flagged",
  ALL = "all",
}
