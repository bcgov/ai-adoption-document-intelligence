export enum DocumentStatusFilter {
  EXTRACTED = "extracted",
  ALL = "all",
}

export enum ReviewStatusFilter {
  PENDING = "pending",
  REVIEWED = "reviewed",
  FLAGGED = "flagged",
  ALL = "all",
}
