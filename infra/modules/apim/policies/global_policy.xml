<policies>
    <inbound>
        <!-- Extract real client IP from X-Forwarded-For -->
        <choose>
            <when condition="@{
                var xff = context.Request.Headers.GetValueOrDefault("X-Forwarded-For", "").Trim();
                return !string.IsNullOrEmpty(xff);
            }">
                <set-header name="X-Real-Client-IP" exists-action="override">
                    <value>@{
                        var xff = context.Request.Headers.GetValueOrDefault("X-Forwarded-For", "").Trim();
                        return xff.Contains(",") ? xff.Split(',')[0].Trim() : xff;
                    }</value>
                </set-header>
            </when>
            <otherwise>
                <choose>
                    <when condition="@(!string.IsNullOrEmpty(context.Request.IpAddress))">
                        <set-header name="X-Real-Client-IP" exists-action="skip">
                            <value>@(context.Request.IpAddress)</value>
                        </set-header>
                    </when>
                </choose>
            </otherwise>
        </choose>

        <!-- Correlation ID for request tracing -->
        <set-header name="x-ms-correlation-request-id" exists-action="skip">
            <value>@(context.RequestId.ToString())</value>
        </set-header>
    </inbound>
    <backend>
        <forward-request buffer-request-body="false" />
    </backend>
    <outbound>
        <set-header name="x-ms-request-id" exists-action="override">
            <value>@(context.RequestId.ToString())</value>
        </set-header>
    </outbound>
    <on-error>
        <choose>
            <when condition="@(context.Response.StatusCode == 429)">
                <return-response>
                    <set-status code="429" reason="Too Many Requests" />
                    <set-header name="x-ms-request-id" exists-action="override">
                        <value>@(context.RequestId.ToString())</value>
                    </set-header>
                    <set-header name="Retry-After" exists-action="override">
                        <value>@(context.Response.Headers.GetValueOrDefault("Retry-After", "30"))</value>
                    </set-header>
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>@{
                        var retryAfter = context.Response.Headers.GetValueOrDefault("Retry-After", "30");
                        return new JObject(
                            new JProperty("error", new JObject(
                                new JProperty("code", "429"),
                                new JProperty("message", "Too Many Requests - Rate limit exceeded"),
                                new JProperty("retryAfter", retryAfter),
                                new JProperty("requestId", context.RequestId.ToString())
                            ))
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <otherwise>
                <return-response>
                    <set-status code="@(context.Response.StatusCode)" reason="@(context.Response.StatusReason)" />
                    <set-header name="x-ms-request-id" exists-action="override">
                        <value>@(context.RequestId.ToString())</value>
                    </set-header>
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>@{
                        var statusCode = context.Response.StatusCode.ToString();
                        var statusReason = context.Response.StatusReason ?? "Error";
                        statusReason = System.Text.RegularExpressions.Regex.Replace(
                            statusReason,
                            @"https?://[a-zA-Z0-9\-]+\.(cognitiveservices\.azure\.com|openai\.azure\.com|search\.windows\.net|blob\.core\.windows\.net|vault\.azure\.net|azure-api\.net)[^\s]*",
                            "[internal-service]");
                        return new JObject(
                            new JProperty("error", new JObject(
                                new JProperty("code", statusCode),
                                new JProperty("message", statusReason),
                                new JProperty("requestId", context.RequestId.ToString())
                            ))
                        ).ToString();
                    }</set-body>
                </return-response>
            </otherwise>
        </choose>
    </on-error>
</policies>
