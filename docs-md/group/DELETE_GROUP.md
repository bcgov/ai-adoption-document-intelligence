# Delete Group API

## Endpoint

`DELETE /api/users/groups/:groupId`

Deletes an existing group by ID.

### Response
- `200 OK` with success message
- `404 Not Found` if group does not exist

## Description
Deletes the specified group and removes related mappings.
