# Get All Groups API

## Endpoint

`GET /groups` (actual)
`POST /api/users/groups/all` (current)

Returns a list of all groups.

### Response
- `200 OK` with array of group objects

## Description
Retrieves all groups from the database.
