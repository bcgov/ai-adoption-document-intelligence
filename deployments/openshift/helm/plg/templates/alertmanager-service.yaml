apiVersion: v1
kind: Service
metadata:
  name: {{ include "plg.alertmanager.fullname" . }}
  labels:
    {{- include "plg.alertmanager.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.alertmanager.httpPort }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "plg.alertmanager.selectorLabels" . | nindent 4 }}
