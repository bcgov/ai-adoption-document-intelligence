apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "plg.grafana.fullname" . }}-dashboards
  labels:
    {{- include "plg.grafana.labels" . | nindent 4 }}
data:
  application-overview.json: |-
    {{- .Files.Get "dashboards/application-overview.json" | nindent 4 }}
