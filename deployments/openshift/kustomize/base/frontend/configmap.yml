apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app: frontend
data:
  # These values are used as build-time environment variables
  # Actual values should be set via kustomize patches or build args
  VITE_API_BASE_URL: ""
  VITE_APP_NAME: "AI OCR Frontend"
  VITE_APP_VERSION: "1.0.0"

