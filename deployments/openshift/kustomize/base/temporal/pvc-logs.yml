apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temporal-worker-logs
  labels:
    app: temporal-worker
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: netapp-file-standard
  resources:
    requests:
      storage: 1Gi
