[
  {
    "id": "B-001",
    "pass": "B",
    "title": "No canvas-level undo/redo anywhere in the editor",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/rich-widgets/ConfusionMapEditor.tsx:102",
    "surfaces": ["page-shell", "canvas", "settings-panel", "workflow-settings"],
    "disposition": "fix",
    "rationale": "`rg -ni 'undo|redo' apps/frontend/src/features/workflow-builder/ -g '!*.test.*'` returns exactly two files. The ConfusionMapEditor hit is the word 'undo' inside a code comment about React state re-sync (cited line) — not an implementation. The ChildWorkflowNodeSettings.tsx hits the probe found are a substring false positive: `decla-redO-utputs` in the identifier `declaredOutputs` (`:511`, `:593`, `:594`, `:616`, `:622`). So the editor has ZERO undo code, not 'two localised undo implementations'. There is no history stack, no command object, no snapshot ring: `setConfig` is a raw React `useState` setter threaded through every mutation path (`WorkflowEditorV2Page.tsx`), and every mutating surface (canvas connect, port drag-to-bind, delete, auto-arrange, node swap, ctx rename, ctx delete, group create/delete, every settings form) calls `onConfigChange` with a whole new config and discards the previous one. Undo is the single load-bearing safety property of a direct-manipulation graph editor — every other destructive finding in this pass (B-002, B-005, B-010, B-012) is only tolerable behind it. It also cannot be retrofitted cheaply once more surfaces mutate config directly, so it is a blocker rather than a deferrable polish item."
  },
  {
    "id": "B-002",
    "pass": "B",
    "title": "Node deletion cascades irreversibly with no confirmation and no undo",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:730",
    "surfaces": ["canvas", "node-menu", "settings-panel", "page-shell"],
    "disposition": "fix",
    "rationale": "Three delete paths exist and none of them asks: the settings-panel trash button (`deleteSelected`, cited line, single node), the context menu's 'Delete node' (`NodeContextMenu.tsx` → `deleteNodeFromContextMenu` at `WorkflowEditorCanvas.tsx:2515`), and bare Delete/Backspace on the canvas (`deleteKeyCode={['Delete','Backspace']}`, `WorkflowEditorCanvas.tsx:3079`, which also deletes a whole multi-selection through `handleDelete` at `:2371`). Each one silently discards the node plus every edge touching it, its group membership, any exposedParams pointing at it, and — if it was the entry node — reassigns `entryNodeId` to whatever `Object.keys(next)[0]` happens to be. With B-001 unfixed there is no way back: a mis-aimed Backspace while the canvas has focus destroys an arbitrary amount of configured work, including rich-widget rule sets (classification rules, validation rules, confusion maps) that took real effort to author. The inconsistency is self-documenting: group deletion DOES guard, with a `window.confirm` whose biome-ignore comment claims native confirm 'matches existing UX patterns elsewhere in the editor for accidental-deletion guards' (`settings/group/GroupNodeSettings.tsx:190`) — a pattern node deletion does not follow. Fix = undo (B-001) or, failing that, a confirm on multi-node and entry-node deletes."
  },
  {
    "id": "B-003",
    "pass": "B",
    "title": "No way to duplicate a node or a selection",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/NodeContextMenu.tsx:15",
    "surfaces": ["node-menu", "canvas", "palette", "hover-extend"],
    "disposition": "fix",
    "rationale": "`rg -ni 'duplicate|clone' apps/frontend/src/features/workflow-builder/ -g '!*.test.*'` returns only ConfusionMapEditor duplicate-KEY warnings, derive-wires' duplicate-writer comments, and a palette comment — no node duplication anywhere. The node context menu offers exactly three entries (cited doc comment enumerates them: Change activity type / Edit script / Delete node). Every node-creation path produces a BLANK node: `addActivity`/`addDynamicNode` materialise schema defaults only (`WorkflowEditorV2Page.tsx:700`), and `hover-extend` calls the same skeleton builders. So the only way to get a second `document.validateFields` node carrying the same 8 hand-authored rules, or a second `ocr.characterConfusion` with the same confusion map, is to re-enter the entire rich-widget payload by hand. This is the single most common authoring action in every comparable graph editor and its absence scales linearly with how much parameter surface an activity has — which, for this catalog's rich widgets, is a lot."
  },
  {
    "id": "B-004",
    "pass": "B",
    "title": "No copy/paste, and no mechanism of any kind to move authored nodes between workflows",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:1090",
    "surfaces": ["canvas", "topbar:more-menu", "save-as-library", "templates-modal", "page-shell"],
    "disposition": "fix",
    "rationale": "No clipboard code exists: `rg -ni 'onPaste|clipboard|ctrl\\+c|ctrl\\+v'` over the feature returns only `IconClipboardList` (the Run-history menu icon) and `dataTransfer.dropEffect = 'copy'` (palette drag). Cross-workflow transfer has no substitute either. The More menu (cited dropdown, `:1090`–`:1184`) has no Export, no Import, no Duplicate workflow; `WorkflowListPage.tsx` has Open/Run/Delete but no duplicate (`rg -ni 'duplicate|clone|copy' apps/frontend/src/pages/WorkflowListPage.tsx` is empty); and there is no JSON download anywhere in the feature (`rg -ni 'download|createObjectURL'` finds only two catalog emoji entries). The two adjacent affordances are not substitutes: 'Save as library' serialises the ENTIRE current config (`handleSaveAsLibrary`, `WorkflowEditorV2Page.tsx:816` — `stripRedundantLocks(config)`, whole graph) and yields a by-reference childWorkflow, not pasteable nodes; templates only seed a brand-new workflow. Net effect: a three-node OCR-cleanup chain perfected in workflow A can only reach workflow B by being rebuilt from the palette, node by node, parameter by parameter."
  },
  {
    "id": "B-005",
    "pass": "B",
    "title": "No way to find a node in a large graph — the editor has search for the catalog but not for the graph",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/palette/ActivityPalette.tsx:109",
    "surfaces": ["canvas", "topbar", "palette", "page-shell", "node-picker"],
    "disposition": "fix",
    "rationale": "Nothing in the editor searches `config.nodes`. The only navigation aids on the canvas are xyflow's `<MiniMap pannable zoomable />` and `<Controls>` (`canvas/WorkflowEditorCanvas.tsx:3083`, `:3082`) — spatial, unlabelled, and useless for 'where is the node that writes `preparedFileData`'. The gap is sharpened by the fact that the environment already implements search one panel over: the palette has a live query field filtering activities, sources, control-flow and dynamic entries (cited `const [query, setQuery] = useState(\"\")` at `:109`, with `filteredDynamicEntries`/`filteredSourceEntries`/`filteredControlFlowEntries` at `:119`/`:130`/`:141`). `NodePicker` even concedes graphs get large — it swaps Select for Autocomplete above 20 nodes (`graph-widgets/NodePicker.tsx:38`) — but that is a form control for picking a reference, not a way to navigate the canvas to a node. There is also no outline/tree view; the topbar shows a bare node/edge count only."
  },
  {
    "id": "B-006",
    "pass": "B",
    "title": "Validation-drawer node selection uses the plain setter the code itself documents as broken, and never pans the node into view",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:1205",
    "surfaces": ["validation-drawer", "canvas", "page-shell", "topbar:validation-button"],
    "disposition": "fix",
    "rationale": "The page defines `selectNodeSticky` precisely because a bare `setSelectedNodeId` does not hold — its own comment reads: 'A plain `setSelectedNodeId` alone doesn't hold — xyflow reasserts its internal (empty) selection on the next change event and clobbers it (the long-standing reason drawer/programmatic selection never focused a node)' (`WorkflowEditorV2Page.tsx:241`). Yet `<ValidationDrawer onSelectNode={setSelectedNodeId} />` (cited line) passes exactly that plain setter, i.e. the drawer's own known-broken path was never migrated, while its sibling prop on the same element uses the fixed helper (`onFixNodeInput={handleFixNodeInput}` at `:1206`, which routes through `selectNodeSticky`). Second, independent half: even when selection sticks, nothing moves the viewport. `handleJumpToProducer` (`:266`) is the ONLY caller of `instance.setCenter` / `fitView`-on-one-node, and it is wired to settings-panel producer rows, not to the drawer. Per INVENTORY §5.8, 30 of the 32 validation anchor shapes fall back to exactly this select-node-and-close behaviour — so for most validation errors, on a graph larger than one screen, clicking the issue produces no visible response at all."
  },
  {
    "id": "B-007",
    "pass": "B",
    "title": "Moving a multi-selection does not persist the other nodes' positions",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/WorkflowEditorCanvas.tsx:2151",
    "surfaces": ["canvas", "auto-arrange", "page-shell"],
    "disposition": "fix",
    "rationale": "xyflow types the drag callback as `OnNodeDrag = (event, node, nodes) => void` (node_modules/@xyflow/react/dist/esm/types/nodes.d.ts:36) — the third argument is the full dragged set. `handleNodeDragStop` destructures only `(_event: React.MouseEvent, node: Node)` (cited line) and writes `metadata.position` for that single `node` (`:2196`, `:2205`). It then bumps `lastFingerprintRef` by hand (`:2204`) so the structural-sync effect will NOT re-project — meaning the other selected nodes keep their new on-screen positions locally while their config positions are stale, and the divergence is invisible until reload. `onSelectionDragStop` (a distinct prop, `component-props.d.ts:144`) is not wired at all, so dragging via the selection-box handle persists nothing whatsoever. Repro: shift-drag a box over three nodes, drag them 200px right, hit Save, reload — the two non-grabbed nodes snap back. This is the only multi-select operation the editor claims to support besides delete and 'Group selected' (`WorkflowEditorV2Page.tsx:1138`), and it is silently lossy."
  },
  {
    "id": "B-008",
    "pass": "B",
    "title": "No align / distribute / snap for hand-placed nodes; the only layout tool rewrites every position at once",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/auto-layout.ts:239",
    "surfaces": ["canvas", "auto-arrange", "topbar:more-menu"],
    "disposition": "defer",
    "rationale": "Layout is all-or-nothing: `handleAutoArrange` (`WorkflowEditorV2Page.tsx:389`) calls `layoutGraphWithMapBodies` (cited line), a dagre pass that stamps a fresh `metadata.position` on EVERY node; `<ReactFlow>` sets no `snapToGrid`/`snapGrid` (`WorkflowEditorCanvas.tsx:3050`–`:3079`), and there is no align-left/distribute-horizontally action on any surface. So a user who has hand-tuned a sub-region either tidies it pixel-by-pixel or loses the whole arrangement to dagre — which, with B-001 unfixed, is itself an unundoable destructive action from a single menu click. Deferred rather than fixed because auto-arrange plus free drag is a defensible minimum for graphs of this size, and the sharper edge here (auto-arrange being irreversible) is subsumed by B-001."
  },
  {
    "id": "B-009",
    "pass": "B",
    "title": "`errorPolicy` has no editor: error handling cannot be authored, and a config that has it is stuck with an unfixable validation error",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/WorkflowEditorCanvas.tsx:592",
    "surfaces": ["settings-panel", "canvas:node-handle", "canvas:wire", "edge-picker", "validation-drawer"],
    "disposition": "fix",
    "rationale": "`errorPolicy` is read-only across the entire frontend: `rg -n 'errorPolicy' apps/frontend/src --glob '!*.test.*'` outside `canvas/` returns exactly one hit, a doc comment. No settings component writes it, and `palette/control-flow-skeletons.ts` never seeds it. The bottom error handle is gated on it — `const showErrorHandle = errorPolicy?.onError === 'fallback'` (cited line; handle rendered at `:662`) — so on any editor-authored node the handle never appears and an error edge cannot be drawn at all. Worse, the path is broken even when a config arrives with `onError: 'fallback'` set by the agent/API/a template: dragging from the error handle stamps `type: 'error'` on the new edge (`:2693`–`:2712`) but never writes `errorPolicy.fallbackEdgeId`, so the core validator permanently reports 'Node \"<id>\" requires fallbackEdgeId when onError is \"fallback\"' (`packages/graph-workflow/src/validator/validator.ts:336`) — an error whose anchor `nodes.<id>.errorPolicy.fallbackEdgeId` routes to the node bucket, selects the node, and lands the user on a settings panel with no errorPolicy section. The widget that would fix it already exists and even names this use case in its own doc comment — 'humanGate.fallbackEdgeId, and errorPolicy.fallbackEdgeId' (`graph-widgets/EdgePicker.tsx:13`) — but is only ever mounted by `HumanGateNodeSettings.tsx:321` and `SwitchNodeSettings.tsx:174`. Blocker: one of the three edge flavours the model defines is unreachable from the editor, and the failure mode is an unclearable red validation state."
  },
  {
    "id": "B-010",
    "pass": "B",
    "title": "Deleting a ctx declaration performs no reference sweep, shows no usage count, and asks nothing",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/WorkflowSettingsDrawer.tsx:187",
    "surfaces": ["workflow-settings", "settings-panel:inputs", "validation-drawer", "canvas:wire"],
    "disposition": "fix",
    "rationale": "`deleteKey` is a two-line object-rest removal (cited line) fired straight from a trash `ActionIcon` (`:343`, `aria-label={'Remove ' + ctxKey}`) with no guard. The asymmetry inside one component is the tell: the SIBLING operation, rename, is backed by a full graph sweep — `renameCtxKeyInConfig` rewrites bindings, map/join ctx keys, childWorkflow mappings and every `ValueRef.ref` inside switch/pollUntil conditions (`settings/rename-ctx-key.ts:157`) — and the drawer even advertises it in body copy: 'Renaming a key rewrites every binding that references it' (`:156`). Delete gets none of that. Every `inputs[].ctxKey` / `outputs[].ctxKey`, `MapNode.collectionCtxKey`, `JoinNode.resultsCtxKey`, childWorkflow mapping and condition `ref` naming the removed key survives, dangling, and surfaces later as `nodes.<id>.inputs[i].ctxKey` errors from `validator.ts:681`. The user gets no warning at the moment of the decision, no 'used by 6 bindings' count, and (B-001) no undo. The rename sweep is already written — reusing it to enumerate references before deleting is the cheap fix."
  },
  {
    "id": "B-011",
    "pass": "B",
    "title": "Ctx-key rename sweep misses library port paths",
    "severity": "minor",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/rename-ctx-key.ts:157",
    "surfaces": ["workflow-settings", "library-port-editor", "save-as-library"],
    "disposition": "fix",
    "rationale": "`renameCtxKeyInConfig` (cited) rewrites `config.ctx` and, per node, `inputs`/`outputs`, map ctx keys, `join.resultsCtxKey`, childWorkflow input/output mappings, and condition ValueRefs. It never touches `config.metadata.inputs[]` / `metadata.outputs[]` — the `LibraryPortDescriptor.path` values (`packages/graph-workflow/src/types.ts:83`), which are ctx keys and are validated as such (`packages/graph-workflow/src/validator/validator.ts:1632`). So on a library-kind workflow, renaming a ctx key in `workflow-settings` silently dangles its declared library ports, and the resulting error anchors at `metadata.inputs[i].path`, which per INVENTORY §5.8 lands in the workflow-level bucket with no deep-link back to the port editor. Minor only because it needs a library-kind workflow to bite, but it is a one-line-per-array addition to a function whose whole stated purpose (its header comment lists the four categories it was extended to cover) is to be exhaustive."
  },
  {
    "id": "B-012",
    "pass": "B",
    "title": "Switch cases cannot be reordered even though the UI states evaluation is order-dependent",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/control-flow/SwitchNodeSettings.tsx:137",
    "surfaces": ["settings-panel:switch", "condition-editor", "canvas:edge-label"],
    "disposition": "fix",
    "rationale": "The panel tells the user order matters — 'Cases are evaluated in order.' (cited line) — and then offers only append (`addCase`, `:103`) and remove-by-index (`removeCaseAt`, `:105`). There is no move-up/move-down, no drag handle, no reorder of any kind (`rg -ni 'reorder|move up|IconGripVertical|drag'` across `settings/rich-widgets/`, `settings/group/` and `sources/FieldListEditor.tsx` returns a single unrelated ConfusionMapEditor comment). To insert a new case ahead of an existing one — the ordinary way a decision table evolves — the user must delete the later cases and re-author each condition tree in the recursive `ConditionExpressionEditor`, then re-point each `SwitchCase.edgeId`. The component even notes cases 'have no stable id and are an ordered list editable by index' (`:148`–`:149`), so the ordering is load-bearing and unmanaged. Same class of hole applies to every other ordered list editor in the feature (page ranges, keyword patterns, classification rules, validation rules, source fields), but switch is where order changes semantics."
  },
  {
    "id": "B-013",
    "pass": "B",
    "title": "An edge's type cannot be changed after it is drawn",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/WorkflowEditorCanvas.tsx:2693",
    "surfaces": ["canvas:wire", "wire-menu", "canvas:edge-label"],
    "disposition": "defer",
    "rationale": "`GraphEdge.type` is decided once, at connect time, from the source node type and source handle id (cited block: switch source → `conditional`, `sourceHandle === 'error'` → `error`, else `normal`) and is never editable afterwards. `WireContextMenu` offers View data / Revert to automatic / Disconnect and opens only for data wires — structural wires 'keep the browser's native context menu' (`WorkflowEditorCanvas.tsx:2430`), so a conditional or error edge has no menu at all. Converting a normal edge to an error edge therefore means deleting it and redrawing from the error handle, which B-009 makes impossible anyway. Deferred: with B-009 fixed, the retype case becomes rare enough that delete-and-redraw is acceptable; without it, this is a symptom rather than the disease."
  },
  {
    "id": "B-014",
    "pass": "B",
    "title": "An inline child workflow — a full graph artifact — is editable only as hand-written JSON in a textarea",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/control-flow/ChildWorkflowNodeSettings.tsx:266",
    "surfaces": ["settings-panel:child-workflow", "canvas:control-flow-card", "page-shell"],
    "disposition": "fix",
    "rationale": "`workflowRef.type === 'inline'` embeds a complete `GraphWorkflowConfig` (`packages/graph-workflow/src/types.ts:258`), and the entire editing surface for it is a JSON `Textarea` bound to a string draft (cited `value={inlineDraft}` / `onChange={commitInlineDraft}`; parse-and-commit at `:161`–`:176`, hint copy 'Edit the inline child graph as JSON' at `:256`). Inside that textarea the user has no palette, no canvas, no port rows, no auto-wire, no kind checking at the picker level, no preview, no validation drawer scoping — none of the machinery Parts 3–9 exist to provide, for an artifact of exactly the same type as the outer graph. The component's own header comment concedes it: 'a dimmed hint that inline graph editing is …' (`:13`). This is the one place where the visual editor's central premise (graphs are authored visually) is dropped without a stated reason."
  },
  {
    "id": "B-015",
    "pass": "B",
    "title": "No extract-to-sub-workflow and no inline-a-sub-workflow — the two refactors that make composition usable",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:816",
    "surfaces": ["topbar:more-menu", "save-as-library", "settings-panel:child-workflow", "canvas", "settings-panel:group"],
    "disposition": "fix",
    "rationale": "The system supports composition (childWorkflow nodes, library workflows, `LibraryPickerModal`) but offers no refactor into or out of it. Extract: 'Save as library' takes the WHOLE config — `const persisted = stripRedundantLocks(config)` (cited line), never a selection — so a user who has built a reusable 4-node cleanup chain inside a larger workflow cannot promote just that chain; there is no 'Extract selection to sub-workflow' on the More menu (`:1090`–`:1184`), the node context menu (`canvas/NodeContextMenu.tsx:15`) or the group panel, even though `selectedNodeIds` and `NodeGroup` already give the selection and the boundary. Inline: nothing expands a childWorkflow back into its parent — `rg -ni 'extract to|extract selection|inline the|expand inline'` over the feature returns nothing. Both directions are the standard escape hatches when a decomposition turns out wrong; without them, the choice of where a boundary sits is made once and permanently."
  },
  {
    "id": "B-016",
    "pass": "B",
    "title": "The per-node preview can only ever show the node's FIRST output binding",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/WorkflowEditorCanvas.tsx:1269",
    "surfaces": ["preview-widget", "canvas:node-card", "canvas:port-rows", "wire-peek"],
    "disposition": "fix",
    "rationale": "The canvas projects `primaryOutputCtxKey: node.outputs?.[0]?.ctxKey` (cited line) and hands only that to `<NodePreviewOverlay outputCtxKey={data.primaryOutputCtxKey} />` (`:870`); `PreviewWidget` reads the cached delta at that single key via `resolveCtxBinding` (`preview/PreviewWidget.tsx:235`) and renders whatever comes back. Nodes with more than one output port — the whole reason `canvas:port-rows` renders one row per port — therefore have every output after the first invisible during a run: a split-and-classify producing both segments and a classification shows one of them, with no affordance to switch. The prop's own doc comment states the limit rather than justifying it ('The ctx key the node binds its (first) output to', `preview/PreviewWidget.tsx:243`–`:244`). The workaround (`wire-peek`) only exists where a data wire was actually drawn to a consumer, so an unconsumed second output is unobservable. Fix = let the preview follow the selected port row, or render one pane per bound output."
  },
  {
    "id": "B-017",
    "pass": "B",
    "title": "'Why didn't this node run' is explained only in replay — during a live Try the editor says nothing at all",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/preview/PreviewWidget.tsx:199",
    "surfaces": ["preview-widget", "run-status-badge", "canvas:node-card", "run-state"],
    "disposition": "fix",
    "rationale": "The honest not-run copy is gated behind `if (isReplay && runId !== undefined && runId !== '')` (`preview/PreviewWidget.tsx:170`); the live-Try branch is a bare `return null` (cited line) with the comment 'stay silent so the canvas isn't cluttered'. So in the moment that actually matters — watching a Try you just launched take an unexpected branch — a node that was skipped, never reached, or failed before producing output shows an empty card and nothing else. Even in replay the explanation collapses four distinct causes into one sentence: `notRunMessage` (`:58`–`:65`) returns the same 'This step didn't run in this run' for pending, running, cancelled AND absent, and its own comment admits the conflation ('the branch was not taken or the run never reached this node', `:62`–`:63`). Per INVENTORY §5.2 the concept has no named literal and no type — it is a computed copy string. For a switch-heavy workflow, 'which branch did it take and why' is THE debugging question, and the editor answers it only after the fact, and only approximately."
  },
  {
    "id": "B-018",
    "pass": "B",
    "title": "Cache-hit metadata is fetched on every poll and rendered nowhere, leaving `skipped` ambiguous",
    "severity": "minor",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/node-status.types.ts:57",
    "surfaces": ["run-status-badge", "preview-widget", "canvas:node-card", "run:polling"],
    "disposition": "fix",
    "rationale": "`NodeRunStatus.cacheHit` (cited line) carries `{ configHash, inputHash }` and its doc comment states the intent outright: 'the Phase 4 cache decorator (US-128) records which cache row served the output so the canvas can surface \"served from cache\" UX' (`:35`–`:38`). No surface reads it — `rg -n 'cacheHit' apps/frontend/src --glob '!*.test.*'` returns only this declaration. Meanwhile `skipped` renders as a violet `IconBolt` with no tooltip and no distinguishing text (`run/NodeStatusBadge.tsx:62`; the tooltip branch at `:118` fires only for `failed`), and `PreviewWidget.producedOutput` treats `skipped` as 'produced output' (`preview/PreviewWidget.tsx:49`). So 'skipped because a cached row was reused' and 'skipped because the branch wasn't taken' are the same violet dot. That directly undercuts test-plan Part 9.6's cache story: the user cannot tell whether a re-Try actually hit cache. The data is already on the wire — this is a rendering gap, not a plumbing one."
  },
  {
    "id": "B-019",
    "pass": "B",
    "title": "No unsaved-changes guard: a reload or a stray back-navigation discards the entire editing session",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "Repro: open /workflows/<id>/edit, add three nodes from the palette, wire them, edit parameters — do not click Save — then press the browser Back button or F5. All edits are gone with no prompt. Verification: `rg -rn 'beforeunload' apps/frontend/src` returns nothing (no window listener anywhere in the frontend), and there is no react-router blocker/usePrompt in the feature — `rg -ni 'beforeunload|unsaved|isDirty|blocker|usePrompt' apps/frontend/src/features/workflow-builder/ -g '!*.test.*'` returns only three prose comments (WorkflowEditorV2Page.tsx:505, :507, :874), none of which installs a guard. Config lives solely in React state (setConfig in WorkflowEditorV2Page.tsx) with no draft persisted to localStorage or the server.",
    "surfaces": ["page-shell", "topbar", "canvas"],
    "disposition": "fix",
    "rationale": "The page KNOWS when it is dirty — the hydration effect compares `configRef.current !== lastHydratedConfigRef.current` to decide whether a server refetch may safely stomp local edits (`WorkflowEditorV2Page.tsx:513`–`:518`), and the comment at `:505` names the risk explicitly ('would stomp the user's unsaved canvas edits'). That same dirty bit is never used to protect the user from themselves. There is no autosave, no draft, no beforeunload, no router block. Combined with B-001 (no undo) this means the editor has no durability story of any kind between saves: an accidental navigation is indistinguishable from an accidental delete, and both are unrecoverable. Blocker because the cost is unbounded — an entire authoring session — and the detection logic already exists."
  },
  {
    "id": "B-020",
    "pass": "B",
    "title": "A save rejected by the backend validator loses every error detail on the way to the toast",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/data/services/api.service.ts:166",
    "surfaces": ["topbar", "page-shell", "validation-drawer"],
    "disposition": "fix",
    "rationale": "The backend rejects an invalid config with a structured body — `throw new BadRequestException({ message: 'Invalid workflow configuration', errors: validation.errors })` (`apps/backend-services/src/workflow/workflow.service.ts:734`–`:738`) where `errors` is the `GraphValidationError[]` carrying path/message/severity per node. The frontend transport extracts `message` and nothing else (cited line and the branches at `:168`–`:179`; the sibling `errors` key is never read), `useUpdateWorkflow` re-wraps that string (`apps/frontend/src/data/hooks/useWorkflows.ts:167`), and `handleSave`'s catch renders it as a red notification (`WorkflowEditorV2Page.tsx:794`–`:800`). Result: 'Save failed — Invalid workflow configuration', with no node, no path, and nothing routed into `validation-drawer`. This is not covered by the client-side validator, because the backend runs the dynamic-node-aware pass (`validateGraphConfigWithDynamicNodes`, `workflow.service.ts:728`) that the client cannot reproduce — so there is a real class of saves that fail with zero actionable feedback and no retry path other than guessing."
  },
  {
    "id": "B-021",
    "pass": "B",
    "title": "Two tabs on one workflow silently lose the first author's changes — no lost-update detection on save",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/workflow/workflow.controller.ts:1454",
    "surfaces": ["page-shell", "topbar", "version-history"],
    "disposition": "fix",
    "rationale": "`PUT /workflows/:id` accepts `@Param('id')` and `@Body() dto: Partial<CreateWorkflowDto>` only (cited signature) — no If-Match, no ETag, no expected-version field on the DTO. The service appends a version on top of whatever head happens to be at commit time (`apps/backend-services/src/workflow/workflow.service.ts:764`–`:788`: read head, compare configs, `version_number + 1`, repoint `head_version_id`) and its only conflict handling is to RETRY on the version-number unique constraint (`:802`–`:813`), which makes silent clobbering more reliable rather than less. On the client, the hydration guard deliberately refuses to adopt server state while local edits exist (`apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:513`), so tab B never learns tab A saved. Sequence: both tabs load v5, A saves (v6), B saves (v7 built from v5) — A's work is gone from head, with no warning to either party. The mitigation is real but manual and after-the-fact: 'Revert to this version' in the history drawer (`versioning/VersionHistoryDrawer.tsx:206`) requires noticing the loss first. Fix = send the loaded `version_number` and 409 on mismatch; the versioning model already stores everything needed."
  },
  {
    "id": "B-022",
    "pass": "B",
    "title": "Real-time collaborative editing (presence, live cursors, operational merge) is out of scope",
    "severity": "minor",
    "type": "non-goal",
    "evidence": "apps/backend-services/src/workflow/workflow.service.ts:778",
    "surfaces": ["page-shell", "canvas", "version-history"],
    "disposition": "won't-support",
    "rationale": "Recorded deliberately so it stops being rediscovered as a hole. The persistence model is an append-only chain of immutable, whole-config versions (cited `tx.workflowVersion.create` writing the entire `config` as one JSON blob, head repointed at `:785`–`:788`). Multi-user live editing would require per-field operations, a merge strategy and a presence channel — a different persistence model, not a missing feature. Nothing in the roster's item 11 obliges a workflow authoring tool at this stage to be Figma. What IS obligatory from item 11, and is a genuine gap, is detecting the lost update rather than preventing it — filed separately as B-021. Distinguishing the two matters: B-021 is a small, cheap fix; this is a rewrite, and choosing not to do it should be a recorded decision."
  },
  {
    "id": "B-023",
    "pass": "B",
    "title": "Diff is side-by-side raw JSON against head only — no structural diff, no arbitrary version pair, no 'what have I changed this session'",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/versioning/CompareToHeadModal.tsx:13",
    "surfaces": ["compare-to-head", "version-history", "topbar:more-menu", "page-shell"],
    "disposition": "defer",
    "rationale": "The modal's own header states the shape and the deferral: 'two JsonInput blocks is the explicit Track 3 deliverable. Structural diff is filed for Phase 4' (cited line). Three consequences for a Parts 3–9 author. (1) Comparing a 40-node config as two raw JSON panes does not answer 'which node changed' — node order in the object, `metadata.position` churn from any drag, and `configHash` restamping all move together with real edits. (2) Only version-vs-head is offered: `handleCompare` passes a single version id (`versioning/VersionHistoryDrawer.tsx:147`) and the Compare button is disabled on head itself (`:219`), so two historical versions cannot be compared. (3) There is no in-session diff — nothing shows the delta between the loaded config and the unsaved working copy, even though `lastHydratedConfigRef` (`WorkflowEditorV2Page.tsx:492`) already holds exactly that baseline. Deferred: the capability is acknowledged and scheduled, and (3) is the piece worth pulling forward because the baseline is already in hand."
  },
  {
    "id": "B-024",
    "pass": "B",
    "title": "The graph cannot be authored without a mouse — zero focusable authoring affordances in the whole feature",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/palette/ActivityPalette.tsx:334",
    "surfaces": ["palette", "canvas", "canvas:port-rows", "hover-extend", "node-menu", "wire-menu"],
    "disposition": "fix",
    "rationale": "`rg -n 'tabIndex' ` and `rg -n 'role=\"'` over `apps/frontend/src/features/workflow-builder/` (excluding tests) each return ZERO hits. Every palette row — the only way to create a node — is a Mantine `<Group>` (a plain div) carrying `onClick` + `draggable` and nothing else: activity rows at the cited line (`onClick` `:337`, `draggable` `:338`), dynamic rows `:441`, source rows `:528`, control-flow rows `:590`. Not focusable, no `role`, no key handler: a keyboard user cannot add a single node. The other authoring gestures are equally mouse-bound — port drag-to-bind via `<Handle>` plus `onMouseEnter`/`onMouseLeave` (`canvas/PortRows.tsx:202`, `:212`, `:221`), `hover-extend` driven purely by hover (`canvas/use-hover-extend.ts`), and both context menus opened from `onNodeContextMenu` / `onEdgeContextMenu` (`canvas/WorkflowEditorCanvas.tsx:3065`–`:3066`). What DOES work comes free from xyflow defaults, not from this code: nodes are focusable and arrow-movable (`disableKeyboardA11y = false`) and Delete/Backspace deletes (`:3079`) — i.e. the one keyboard-reachable operation in the editor is the destructive one. Mantine drawers and forms are natively keyboard-usable, so the gap is specifically the canvas + palette authoring loop."
  },
  {
    "id": "B-025",
    "pass": "B",
    "title": "Breakpoints, stepping, and mid-run intervention are out of scope for the editor",
    "severity": "minor",
    "type": "non-goal",
    "evidence": "apps/frontend/src/features/workflow-builder/run/useNodeStatuses.ts:1",
    "surfaces": ["run-state", "run:polling", "run-drawer", "canvas:node-card"],
    "disposition": "won't-support",
    "rationale": "Recorded so roster item 8 is not read as demanding a step-debugger. Execution happens in Temporal, out of process; the editor observes it through a 1.5 s poll of a node-status endpoint (cited file; INVENTORY §2.7 `run:polling`) and a read-only output cache. A breakpoint would require the engine to offer suspend/resume at node granularity, which is an engine capability, not an editor one — and the product already has a first-class, designed mechanism for deliberate mid-run pausing: the `humanGate` node. Post-hoc inspection (previews, wire peek, run history, replay) is the right shape for this architecture. The genuine item-8 gaps are that the post-hoc view is incomplete — filed as B-016, B-017, B-018 and B-026 — not that stepping is missing."
  },
  {
    "id": "B-026",
    "pass": "B",
    "title": "No way to inspect the run's ctx blackboard as a whole — values are only visible node-by-node or wire-by-wire",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/preview/useActivityOutputPreview.ts:1",
    "surfaces": ["preview-widget", "wire-peek", "workflow-settings", "run-state", "validation-drawer"],
    "disposition": "defer",
    "rationale": "Every observation surface is keyed to one node or one wire: the preview query is per-node (cited hook, batched by node id) and `wire-peek` reads one ctx key off one wire's cached delta (`canvas/WirePeekPopover.tsx`). `workflow-settings` lists ctx DECLARATIONS (name/type/kind/isInput — `settings/WorkflowSettingsDrawer.tsx:159`) but never their runtime values. So questions of the form 'what is in ctx right now' or 'who last wrote `preparedFileData`' require clicking around the canvas and mentally reassembling the blackboard — and for a key whose producer output is not the node's first binding, the value may not be reachable at all (B-016). Deferred rather than fixed: the per-node view covers the common case, and a variables panel is only clearly worth its weight once B-016/B-017 have closed the cheaper holes in the same area."
  },
  {
    "id": "B-027",
    "pass": "B",
    "title": "Accidental-deletion guards are inconsistent across artifacts — group asks, node does not, ctx key does not",
    "severity": "minor",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/group/GroupNodeSettings.tsx:190",
    "surfaces": ["settings-panel:group", "settings-panel", "node-menu", "workflow-settings", "canvas"],
    "disposition": "fix",
    "rationale": "Three deletion guards, three different answers, and the least destructive one is the only guarded one. Deleting a GROUP — which destroys only a presentation grouping and its exposedParams, leaving every node intact — pops a native `window.confirm` whose biome-ignore comment asserts this 'matches existing UX patterns elsewhere in the editor for accidental-deletion guards' (cited line). Deleting a NODE, which cascades to edges, group membership, exposedParams and possibly `entryNodeId`, asks nothing (`WorkflowEditorV2Page.tsx:730`; context menu `canvas/WorkflowEditorCanvas.tsx:2515`; Delete/Backspace `:3079`). Deleting a CTX DECLARATION, which can dangle every binding in the graph, asks nothing (`settings/WorkflowSettingsDrawer.tsx:187`). Meanwhile deleting a whole WORKFLOW uses a proper Mantine confirmation modal (`apps/frontend/src/pages/WorkflowListPage.tsx:318`). The claimed pattern does not exist; picking one and applying it by blast radius is the fix."
  }
]
