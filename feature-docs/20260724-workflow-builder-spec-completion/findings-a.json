[
  {
    "id": "A-001",
    "pass": "A",
    "title": "J1.6 / J2.1 — a workflow can only ever have ONE run in flight: every run start cancels all other running executions of the same lineage",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/temporal/temporal-client.service.ts:686",
    "surfaces": ["run-drawer", "source-upload", "run-history-drawer", "run-state"],
    "disposition": "fix",
    "rationale": "`listRunningInLineage` builds the visibility query `WorkflowLineageId = \"<id>\" AND ExecutionStatus = \"Running\"` — it has no Try/production discriminator. `cancelInFlightTriesForLineage` cancels every id it returns, and BOTH run entry points call it before starting: `POST /:id/runs` (apps/backend-services/src/workflow/workflow.controller.ts:534) and the source-upload path (:728). So starting document #2 cancels document #1, mid-OCR. J1 step 6 (\"run the rest of the folder\") and J2 step 1 (\"feed the whole batch through in one go\") are both unreachable, and J2's \"the counts add up to 240 at all times\" can never hold. The design intent (test plan 9.7, cancel-on-new-Try) is an editor affordance that was implemented on the shared run API, so it silently governs production traffic too."
  },
  {
    "id": "A-002",
    "pass": "A",
    "title": "J1.2 / J2.1 — file intake is strictly one file per upload; there is no multi-file or folder affordance anywhere",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/RunWorkflowDrawer.tsx:557",
    "surfaces": ["run-drawer", "source-upload", "settings-panel:source"],
    "disposition": "fix",
    "rationale": "The Run-drawer Dropzone is `multiple={false}`; the settings-panel \"Upload & Try\" button posts one file (apps/frontend/src/features/workflow-builder/sources/useSourceUpload.ts:7 — \"a single part named `file`\") and the endpoint is a single-file `FileInterceptor`. Priya's folder of 60 and Marcus's batch of 240 have to be uploaded one at a time, and A-001 means each upload kills the previous one. Even the honest workaround (write a script that loops the upload endpoint) is defeated by A-001."
  },
  {
    "id": "A-003",
    "pass": "A",
    "title": "J1.4 / J7.3 — the preview of an OCR step shows a blob POINTER, not the extracted values; the actual field values are never visible in the builder",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "packages/graph-workflow/src/types/kind-schemas.ts:22",
    "surfaces": ["preview-widget", "preview:ocr", "preview:dispatch", "wire-peek"],
    "disposition": "fix",
    "rationale": "`OcrResultSchema` is `{documentId, blobPath, storage:\"blob\", byteLength?, pageCount?, status?}` and its own doc comment says \"a blob POINTER to the full OCR payload, not the payload itself\". Every OCR/correction activity in the catalog (`azureOcr.extract`, `mistral-ocr.process`, `ocr.cleanup`, `ocr.spellcheck`, `ocr.characterConfusion`, `ocr.normalizeFields`, `ocr.enrich`) declares kind `OcrResult` on both sides, so the whole chain moves pointers. `PreviewWidget` renders `outputCtx` verbatim (no blob dereference exists in `GET /:id/preview-cache`), so `OcrResultPreview`'s K/V table shows `blobPath` / `byteLength` / `status`. Priya's stated acceptance test — \"recognising the applicant's name off the page\" — is not achievable, and Dana's whole J7 bisection-by-intermediate-value method collapses at the same point. Note also that the `OcrFields` kind exists in the registry but no catalog activity produces it."
  },
  {
    "id": "A-004",
    "pass": "A",
    "title": "J1.3 — a first-timer landing on an empty canvas gets 41 ungrouped activities and no reachable starting recipe; the template picker lives only on the list page",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/pages/WorkflowListPage.tsx:310",
    "surfaces": ["palette", "templates-modal", "topbar:more-menu", "page-shell"],
    "disposition": "fix",
    "rationale": "`TemplatesPickerModal` is mounted only by `WorkflowListPage`; nothing in the editor (`topbar:more-menu` included) can reach it. Once on `/workflows/create` the only guidance is the palette's \"Search activities…\" box over 41 entries in 12 categories. Priya's step 3 assumption — \"the tool already knows how to read a document, I am choosing from what it can do\" — requires her to guess that a document read is `file.prepare` → `azureOcr.submit` → (`pollUntil` on `azureOcr.poll`) → `azureOcr.extract`. `hover-extend` partly rescues this by offering kind-compatible next nodes off a source handle, but it only fires on hover of a handle she has no reason to hover, and the wildcard `Artifact` output ports of `azureOcr.submit` make its suggestion list unfiltered."
  },
  {
    "id": "A-005",
    "pass": "A",
    "title": "J1.5 — a workflow that extracts and then stores nothing validates as \"Valid\"; nothing warns that a produced result is never consumed or persisted",
    "severity": "major",
    "type": "design-gap",
    "evidence": "packages/graph-workflow/src/validator/validator.ts:1045",
    "surfaces": ["validation-engine", "validation-drawer", "topbar:validation-button"],
    "disposition": "fix",
    "rationale": "The only whole-graph warnings the validator emits are unreachable-node (`nodes.<id>`, :1045) and multi-group membership (:1150). There is no check that a terminal node's output ctx key is read by anything or handed to `ocr.storeResults`. Priya's step 5 explicitly anticipates the failure mode \"the result is being read but not kept anywhere\" — the product gives her a green \"Valid\" badge in exactly that state. She would only discover it by noticing that nothing exists at the end, which is the same problem she was trying to avoid."
  },
  {
    "id": "A-006",
    "pass": "A",
    "title": "J2.2 — there is no batch: no grouping of runs, no aggregate done/running/failed counts, no unit above a single run",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run-history/RunHistoryFilters.tsx:43",
    "surfaces": ["run-history-drawer", "run-history-filters", "run-row", "run-history:query"],
    "disposition": "fix",
    "rationale": "`run-history-drawer` is a flat cursor-paged list of individual executions filtered by status / date range / version only. There is no batch entity in the schema, no correlation id on a run, and no count roll-up anywhere. Marcus's step 2 (\"how many are done, how many still going, how many failed — the counts add up to 240\") has no surface to read. Even the degenerate workaround (filter by date range and count rows by eye) can't distinguish this quarter's batch from a re-run of one file."
  },
  {
    "id": "A-007",
    "pass": "A",
    "title": "J2.3 — nothing distinguishes \"slow\" from \"wedged\": a run shows `running` with a start time and no deadline, heartbeat, or last-progress signal",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/node-status.types.ts:47",
    "surfaces": ["run-row", "run-history-drawer", "run-status-badge", "run:polling"],
    "disposition": "fix",
    "rationale": "`NodeRunStatus` carries `startedAt` / `endedAt` / `errorMessage` / `cacheHit` — no attempt count, no deadline, no heartbeat. `RunRow` shows a status dot and a relative start time. Neither surface exposes the activity's effective `startToClose` timeout (defaulted to `\"2m\"` in the engine at apps/temporal/src/graph-engine/node-executors.ts:313) or its retry budget, so \"is this still working or is it stuck\" is unanswerable without the Temporal UI. This is the exact failure Marcus says burned him before, and it is the step of J2 that a batch surface (A-006) alone would not fix."
  },
  {
    "id": "A-008",
    "pass": "A",
    "title": "J2.4 — a failure reason is a hover tooltip on one node badge of one open run; it is not attached to the document and does not survive a reload",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/NodeStatusBadge.tsx:118",
    "surfaces": ["run-status-badge", "run-history-drawer", "run-row", "validation-drawer"],
    "disposition": "fix",
    "rationale": "`errorMessage` reaches the frontend on the node-statuses wire but is rendered only as a Mantine `<Tooltip label={errorMessage}>` on the failed node's canvas badge — not selectable, not copyable, not listed, and gone as soon as `activeRunId` is cleared (a page reload clears it). `run-row` shows only a red dot. To read the reason for one of Marcus's 240 files he must replay that specific run, find the failed node, and hover it. Distinguishing \"password-protected\" from \"service rejected the content\" from \"not a readable PDF\" — his stated routing requirement — is possible only if the underlying activity happens to phrase the message that way, and there is no place that message is retained."
  },
  {
    "id": "A-009",
    "pass": "A",
    "title": "J2.5 — no export of any kind exists on any run surface",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run-history/RunHistoryDrawer.tsx:1",
    "surfaces": ["run-history-drawer", "run-row", "run-history-filters"],
    "disposition": "defer",
    "rationale": "SPECULATIVE REQUIREMENT: the brief flags J2 step 5 as inferred rather than observed. Confirmed as a wall — `RunHistoryDrawer` renders an infinite-scroll list with a filter row and a Replay button and nothing else; there is no download, copy-all, or CSV affordance in `run-history-drawer`, `run-row`, or `run-history-filters`. Ranking this minor and deferring: the wall is real but downstream of A-006 and A-008 — without a batch and without a retained per-document reason there is nothing to export yet."
  },
  {
    "id": "A-010",
    "pass": "A",
    "title": "J2 edge branch — `errorPolicy` has NO authoring surface: the engine supports fail/fallback/skip and the canvas renders the error handle for it, but nothing in the builder can set it",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/temporal/src/graph-engine/error-handling.ts:24",
    "surfaces": ["settings-panel", "settings-panel:params", "canvas:wire", "node-menu"],
    "disposition": "fix",
    "rationale": "The engine reads `node.errorPolicy?.onError ?? \"fail\"` and honours `fallback` (routes to `fallbackEdgeId`) and `skip`; `retryable === false` is honoured at :76. The canvas mounts the bottom `error` source handle when `errorPolicy?.onError === \"fallback\"` (apps/frontend/src/features/workflow-builder/canvas/WorkflowEditorCanvas.tsx:592), and test-plan 5.2 says \"On a node with `errorPolicy.onError = \\\"fallback\\\"` … draw from it\" — assuming it is already set. A repo-wide grep of `apps/frontend/src` for writers of `errorPolicy` / `onError` finds only reads plus `swap-node-type.ts:164` carrying the field through; `NodeSettingsPanel` renders header, label, params form, Inputs section, and the advanced bindings toggle — nothing else. So the whole of Marcus's edge branch (\"unreadable files set aside with their reason recorded and the remaining 228 finishing normally … describe this once in the workflow\") is unauthorable in the product. Same gap kills J4 step 7. `RetryPolicy` and `TimeoutPolicy` are equally editor-less."
  },
  {
    "id": "A-011",
    "pass": "A",
    "title": "J2 edge branch — no way to exercise the failure path without actually breaking a file: no dry-run, no simulated error, no branch preview",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/RunWorkflowDrawer.tsx:90",
    "surfaces": ["run-drawer", "preview-widget", "canvas:wire"],
    "disposition": "defer",
    "rationale": "`run-drawer` offers exactly two modes, `run` and `try`, both of which execute the real graph against real input. Nothing lets an author force a node to fail so the containment branch is observed. Marcus states the requirement explicitly (\"check that he described it correctly without deliberately breaking a file to test it\"). Deferring behind A-010: there is nothing to rehearse until the policy is authorable."
  },
  {
    "id": "A-012",
    "pass": "A",
    "title": "J2.6 — removing failed documents is not reachable from any Parts 3–9 surface",
    "severity": "minor",
    "type": "non-goal",
    "evidence": "The workflow-builder editor (`/workflows/:id/edit`) exposes palette, canvas, settings panel, validation drawer, run drawer, run-history drawer and version history. None list documents or offer a delete. Document lifecycle lives in the documents module, outside the Parts 3–9 scope this pass covers.",
    "surfaces": ["page-shell", "run-history-drawer"],
    "disposition": "won't-support",
    "rationale": "Recording deliberately so it stops being rediscovered: the builder is a workflow-authoring surface and does not own document records. Marcus's step 6 is a real product requirement but belongs to the documents module, not to Parts 3–9. Flagging for the merge so it is routed rather than dropped."
  },
  {
    "id": "A-013",
    "pass": "A",
    "title": "J3.2 — a keyword split pattern cannot be tried against a real sample before committing; the editor validates regex syntax only",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/rich-widgets/KeywordPatternEditor.tsx:98",
    "surfaces": ["widget:keyword-pattern", "settings-panel:params", "preview:segments"],
    "disposition": "fix",
    "rationale": "`KeywordPatternEditor` renders pattern + segment-type rows with an inline invalid-regex error and nothing else — no sample text, no match count, no \"which pages would this catch\". Marcus's step 2 is explicit: \"He expects to be able to see, on a real sample, which pages each pattern matched before he commits to it.\" The only way to find out is to save, run a package, and read `preview:segments` on the `document.splitAndClassify` node — i.e. after committing, and only if the run reached that node and its cache row is still fresh."
  },
  {
    "id": "A-014",
    "pass": "A",
    "title": "J3.3/J3.5 — per-section branching forces a map+join shape, and a branch that dead-ends inside the map body silently drops that section's result with only a yellow warning",
    "severity": "major",
    "type": "design-gap",
    "evidence": "packages/graph-workflow/src/validator/validator.ts:602",
    "surfaces": ["settings-panel:join", "settings-panel:map", "canvas:map-body-box", "validation:map-body", "validation-drawer"],
    "disposition": "fix",
    "rationale": "A join's `sourceMapNodeId` must resolve to a node whose type is `map` (validated at :602/:610), so \"reassemble the per-section results\" is only expressible as switch-inside-a-map-body. The product's own showcase demo ships two of three branches dead-ending short of the body exit (MANUAL_TEST_PLAN.md 4.15 documents this as intentional), and the only signal is a *warning* from `map-body-validation.ts:38` — Save still succeeds and the run still completes, minus those sections. Marcus's step 4 requirement (\"he does not want an unrecognised page silently dropped\") is exactly the state the product tolerates with a yellow badge."
  },
  {
    "id": "A-015",
    "pass": "A",
    "title": "J3.4 — routing the unmatched section works, and reads correctly on the canvas",
    "severity": "minor",
    "type": "non-goal",
    "evidence": "apps/temporal/src/activities/split-and-classify-document.ts:221",
    "surfaces": ["settings-panel:switch", "canvas:edge-label"],
    "disposition": "won't-support",
    "rationale": "Recorded as a deliberate PASS so the merge does not treat J3 step 4 as an open gap. `splitAndClassify` emits `segmentType: \"unknown\"` for unmatched ranges (and a whole-document `unknown` segment when no marker matches at all), a switch default edge exists, and the edge renders as `otherwise` (edge-labels). The gap in this journey is A-014's dead-end drop, not the unmatched-label handling itself."
  },
  {
    "id": "A-016",
    "pass": "A",
    "title": "J4.3 — map fan-out defaults to UNBOUNDED concurrency; the skeleton omits `maxConcurrency` and the form gives no hint of the service limits it is meant to protect",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/temporal/src/graph-engine/node-executors.ts:598",
    "surfaces": ["settings-panel:map", "palette"],
    "disposition": "fix",
    "rationale": "`const maxConcurrency = node.maxConcurrency || Infinity;` and `buildMapSkeleton` (apps/frontend/src/features/workflow-builder/palette/control-flow-skeletons.ts:60) sets `collectionCtxKey`, `itemCtxKey`, `bodyEntryNodeId`, `bodyExitNodeId` and nothing else. So a map dropped from the palette will launch all 300 pages at once. `MapNodeSettings` does render an optional `maxConcurrency` NumberInput (>= 1), but it is blank by default and its description says nothing about downstream rate limits. Marcus's step 3 (\"he does not want to find out by being throttled\") is exactly the outcome the default produces, and the one control that prevents it is opt-in and unlabelled as a safety setting."
  },
  {
    "id": "A-017",
    "pass": "A",
    "title": "J4.6 — a fanned-out map shows no progress at all: >20 items run as child workflows whose node statuses never reach the parent's query, and <=20 items collapse to last-iteration-wins",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "apps/temporal/src/graph-engine/node-executors.ts:601",
    "surfaces": ["run-status-badge", "run:polling", "canvas:map-body-box", "run-state"],
    "disposition": "fix",
    "rationale": "`useChildWorkflows = collection.length > MAP_CHILD_WORKFLOW_THRESHOLD (20) && workflowVersionId !== undefined` — Marcus's 40-to-300-page documents always take this path, and each iteration runs as a separate Temporal execution via `executeChild(\"graphWorkflow\", …)`; the parent's `nodeRunStatuses` map is not shared with them, so `GET /:id/runs/:runId/node-statuses` reports nothing for any body node. Below the threshold, `executeBranchSubgraph` shares the parent map but keys statuses by node id — its own comment at :1080 says \"Map subgraphs nest the same node ids across iterations — the last iteration's status wins\". Either way the map node itself just sits on `running` for the whole fan-out. There is no iteration counter anywhere (grep of the canvas and `run/` for progress/counter finds none). J4 step 6 — \"tell whether it is progressing and roughly how far along it is\" — has no surface."
  },
  {
    "id": "A-018",
    "pass": "A",
    "title": "J4.7 — one bad page kills the whole document: the map fan-out awaits `Promise.all`, not `allSettled`, and no per-node skip policy is authorable",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/temporal/src/graph-engine/runner-utils.ts:48",
    "surfaces": ["settings-panel:map", "settings-panel", "run-status-badge"],
    "disposition": "fix",
    "rationale": "`executeWithConcurrencyLimit` does `await Promise.race(executing)` inside the loop and `await Promise.all(executing)` at the end, so the first rejecting iteration rejects the map and fails the run. Marcus states the opposite requirement: \"he wants the other 299 pages, with the bad page identified — not a lost document.\" The engine's own per-node `errorPolicy.onError: \"skip\"` would express it, but per A-010 there is no way to set it, and even set it would not change the map's all-or-nothing await. `JoinNode.strategy` is fixed to `\"all\"` (types.ts:242) so there is no partial-collect escape either."
  },
  {
    "id": "A-019",
    "pass": "A",
    "title": "J5.5 — a humanGate can never be resumed by the product's own review queue: approving a review session sends no Temporal signal",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/backend-services/src/hitl/hitl.service.ts:407",
    "surfaces": ["settings-panel:human-gate", "run-status-badge", "run-history-drawer"],
    "disposition": "fix",
    "rationale": "`executeHumanGateNode` flips the document to `awaiting_review` (apps/temporal/src/graph-engine/node-executors.ts:783) so the document does appear in the HITL queue, then blocks on `condition(() => payload !== null, node.timeout)`. The queue's approve action posts `/hitl/sessions/:id/submit` → `approveSession`, which updates the session row and sets the document to `complete` (:407) and never touches Temporal. The ONLY code in the repo that signals `humanApproval` is `TemporalClientService.sendHumanApproval` (:1022), whose sole caller is `POST /api/documents/:documentId/approve` (apps/backend-services/src/document/document.controller.ts:839) — an endpoint the review UI does not call, and which 400s unless `document.workflow_execution_id` or `workflow_id` is set (see A-020). Net effect: the gate always times out. With the skeleton default `onTimeout: \"fail\"` the run then fails an hour later. Dana's step 5, and therefore steps 7 and the whole \"done\" definition of J5, are unreachable."
  },
  {
    "id": "A-020",
    "pass": "A",
    "title": "J5.5 — a builder-started run never records its Temporal execution id on the Document it created, so the one endpoint that could resume a humanGate can never find the run",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/backend-services/src/workflow/workflow.controller.ts:717",
    "surfaces": ["source-upload", "settings-panel:human-gate", "run-state"],
    "disposition": "fix",
    "rationale": "The source-upload handler creates the Document with `workflow_id: null` / `workflow_execution_id: null` (:717–:719), then calls `startGraphWorkflow` and returns `runId` to the browser — it never writes `runId` back onto the row. A repo-wide grep for writers of `workflow_execution_id` in `apps/backend-services/src` finds only `null` literals at document creation plus reads. `approveDocument` resolves `document.workflow_execution_id || document.workflow_id` and throws `400 \"does not have an associated workflow execution ID\"` when both are absent. So even the correct-by-hand path (call `/documents/:id/approve` yourself) is closed for any document a builder workflow produced. This is the second, independent break under A-019 — fixing either alone is not enough."
  },
  {
    "id": "A-021",
    "pass": "A",
    "title": "J5.3 — a threshold cannot be varied per group at run time: exposed params are authorable but only the benchmark module can supply values for them",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/backend-services/src/benchmark/workflow-config-overrides.ts:18",
    "surfaces": ["exposed-params-editor", "settings-panel:group", "run-drawer"],
    "disposition": "fix",
    "rationale": "`NodeGroup.exposedParams` is the product's answer to \"a value I can change without touching the structure\", and the engine threads `workflowConfigOverrides` down through map child workflows. But the only code that turns exposed params into overrides lives under `apps/backend-services/src/benchmark/`, and `StartRunRequestDto` accepts only `initialCtx` and `workflowVersionId` (apps/backend-services/src/workflow/dto/start-run.dto.ts:21) — no overrides field, and `run-drawer` renders no exposed-param inputs. Dana's step 3 (\"change it for one group without affecting another\") therefore reduces to editing the node's parameter and cutting a new version, which changes it for everyone."
  },
  {
    "id": "A-022",
    "pass": "A",
    "title": "J5.7 — the advanced-bindings editor cannot ADD a binding to a control-flow node, so there is no way to say where a reviewer's payload lands",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/NodeSettingsPanel.tsx:794",
    "surfaces": ["settings-panel:advanced-bindings", "settings-panel:human-gate", "settings-panel:map"],
    "disposition": "fix",
    "rationale": "`portsForFooter` returns catalog ports for activity nodes but `currentBindings.map(...)` for every control-flow node, and `PortBindingsEditor` renders \"None.\" when that list is empty (:833) with no add-row control. A humanGate dropped from the palette has no `outputs[]`, so its Output-bindings section is permanently empty and the reviewer's payload falls through to the engine's undocumented fallback key `${node.id}Payload` (apps/temporal/src/graph-engine/node-executors.ts:849). Dana's step 7 (\"confirm the corrections a reviewer makes are what gets stored\") cannot even be expressed — she cannot name the ctx key, and the port names she would need (`approved`, `reviewer`, `comments`, `rejectionReason`, `annotations`, from `ApproveDocumentDto`) are nowhere in the UI."
  },
  {
    "id": "A-023",
    "pass": "A",
    "title": "J5.5 — three of the four signal-name presets are dead ends that no caller in the product ever sends",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/settings/control-flow/HumanGateNodeSettings.tsx:90",
    "surfaces": ["settings-panel:human-gate"],
    "disposition": "fix",
    "rationale": "`SIGNAL_NAME_PRESETS = [\"humanApproval\", \"approve\", \"review\", \"reject\"]`. Only `humanApproval` is ever emitted (temporal-client.service.ts:1022, the sole `.signal(` for approval in the repo). Picking `approve` from an autocomplete the product itself offers produces a gate nothing can ever resume. The form's blue info Alert does say \"the HITL flow sends `humanApproval`\" (:245), which is a real improvement over the free-text field this used to be — but the presets still invite the wrong choice one keystroke away."
  },
  {
    "id": "A-024",
    "pass": "A",
    "title": "J5.5 — escalation cannot be modelled: an escalated review session sets a status and stops; nothing routes it to a different person or back to the run",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/hitl/hitl.service.ts:463",
    "surfaces": ["settings-panel:human-gate", "settings-panel:switch"],
    "disposition": "defer",
    "rationale": "`escalateSession` writes `ReviewStatus.escalated` and an audit event and, like `approveSession`, sends no signal. On the authoring side a humanGate has exactly one resume path and one timeout path (`fail`/`continue`/`fallback`) — there is no \"which decision did the reviewer make\" branch and no way to name a second reviewer pool. Dana's \"escalation should reach a different person, not loop back to the same queue\" has no expression in the graph model. Deferring behind A-019/A-020: the ordinary approve path has to work before escalation is meaningful."
  },
  {
    "id": "A-025",
    "pass": "A",
    "title": "J6.1 — the \"logical stages\" overview only exists if the previous author happened to create groups; nothing derives structure from the graph",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/group-projection.ts:57",
    "surfaces": ["canvas:group-chip", "topbar:more-menu", "auto-arrange"],
    "disposition": "fix",
    "rationale": "Simplified view projects one chip per `config.nodeGroups` entry (`chipIdForGroup`). With no groups authored it is a no-op and Sam gets 16 undifferentiated nodes — precisely the state J6 step 1 says must not happen (\"Sam expects the structure to be legible at a scale larger than individual steps\"). Grouping is an optional authoring act by the person who has left; nothing infers stages from reachability, kind families, or `document.*`/`ocr.*` category boundaries, and `auto-arrange` only re-positions."
  },
  {
    "id": "A-026",
    "pass": "A",
    "title": "J6.3/J6.6 — there is no find-in-workflow and no \"what else reads this\" view; the only search in the editor searches the palette",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/palette/ActivityPalette.tsx:212",
    "surfaces": ["canvas", "settings-panel:inputs", "workflow-settings", "topbar"],
    "disposition": "fix",
    "rationale": "Confirmed by reading, not by an empty grep: the only search input in the editor is the palette's `placeholder=\"Search activities...\"`; `WorkflowEditorCanvas.tsx` (grepped with -a, 3180 lines) and `WorkflowEditorV2Page.tsx` contain no search/filter/goto-node affordance. `settings-panel:inputs` shows a node's inbound sources (`← <producer label>`) but there is no reverse list of consumers, and no way to ask \"which nodes set `modelId`\". J6 step 6 — \"check what else that setting is shared with before saving\", which Sam names as his specific fear — requires opening all 16 nodes one at a time. The canvas's derived data wires do render producer→consumer direction, which is a genuine partial answer for step 3, but only for values that flow through a bound port."
  },
  {
    "id": "A-027",
    "pass": "A",
    "title": "J6.5/J7.2 — Replay does NOT load the run's version: it overlays a historical run's statuses and previews onto whatever the canvas is showing now",
    "severity": "blocker",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/WorkflowEditorV2Page.tsx:1526",
    "surfaces": ["run-history-drawer", "run-row", "topbar:replay-indicator", "run-state", "preview-widget"],
    "disposition": "fix",
    "rationale": "`handleReplay` does exactly `setActiveRunId(runId); setIsReplay(true); onClose();` — it never fetches `run.workflowVersionId` and never reloads `config`. `RunSummaryDto` carries the version id and `run-row` even renders a `v3 — head` pin, so the information is on screen and deliberately not used. The overlay is keyed by node id, so on a workflow edited since the run, statuses land on nodes that did not exist in that run and vanish for nodes that have been renamed — with no warning. J7 step 2 is categorical about this: \"a diagnosis against the current version would be worthless.\" The settings panel's replay Alert (\"changes will not affect the displayed historical preview\", NodeSettingsPanel.tsx:934) confirms the editor stays on the live config."
  },
  {
    "id": "A-028",
    "pass": "A",
    "title": "J6.4 — a workflow declares what it expects but not what it produces; `run-spec` has an inputSchema and no output contract",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/workflow/build-run-spec.ts:15",
    "surfaces": ["run-drawer", "workflow-settings", "library-port-editor"],
    "disposition": "defer",
    "rationale": "`RunSpec` is `{triggerUrl, inputSchema, authNotes, sampleCurl}` (+ optional `uploadSpec`). Only library workflows declare outputs, via `metadata.outputs` / `LibraryPortDescriptor`. So the first half of J6 step 4 is served (Run drawer shows the declared input schema) and the second half — \"what it produces at the end\" — is not, unless someone saved the workflow as a library. Minor because Sam can infer it from the terminal nodes' output bindings; deferring pending a decision on whether non-library workflows should carry an output contract at all."
  },
  {
    "id": "A-029",
    "pass": "A",
    "title": "J6.2 — a nested branch condition is unreadable from the canvas: it collapses to `all of (2)` and only the settings form shows the predicate",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/canvas/edge-labels.ts:72",
    "surfaces": ["canvas:edge-label", "condition-editor", "settings-panel:switch"],
    "disposition": "defer",
    "rationale": "Flat comparisons humanise well (`is`, `is not`, `contains`, `≥` — edge-labels.ts:38–46), which meets J6 step 2 for simple cases. Anything with an `and`/`or` renders `all of (N)` / `any of (N)`, so Sam must open the switch's settings form and expand the tree to read it — and the master template's `segmentRouter` is exactly that shape. Deferring: a tooltip carrying the expanded predicate would close this cheaply and it is not on the critical path of any journey."
  },
  {
    "id": "A-030",
    "pass": "A",
    "title": "J6.7 — \"what changed\" is answered with two raw JSON blobs side by side, not a structural diff",
    "severity": "minor",
    "type": "non-goal",
    "evidence": "docs-md/workflow-builder/MANUAL_TEST_PLAN.md:425",
    "surfaces": ["compare-to-head", "version-history"],
    "disposition": "won't-support",
    "rationale": "Test-plan 12.3 states \"modal with two read-only JSON blocks side-by-side (`v{n}` vs `head`); no structural diff (by design)\". Recording it as a deliberate decision so it stops being rediscovered. Sam's step 7 (\"verify the change is what was intended by comparing against how it was before\") is technically answerable by eye on a one-line change, and `version-history` Revert covers \"know how to get back if it was wrong\" cleanly."
  },
  {
    "id": "A-031",
    "pass": "A",
    "title": "J7.1 — a run cannot be found by its document: run history filters on status, date range and version only",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run-history/RunHistoryFilters.tsx:43",
    "surfaces": ["run-history-filters", "run-history-drawer", "run-row", "run-history:query"],
    "disposition": "fix",
    "rationale": "`STATUS_OPTIONS` plus From/To `DateInput`s plus a version Select is the whole filter row; `ListRunsFilters` has no free-text or ctx-value predicate. The only per-run identifier on screen is `run-row`'s `inputCtxSummary` chip, which truncates to the first two keys — for an upload-started run that is a blob storage key and a UUID (workflow.controller.ts:736–739 sets `initialCtx = {<ctxKey>: blobKey, documentId}`), never the filename. Dana's step 1 is explicit that she must find it \"by the document rather than by remembering when it ran\"; in practice she must already know the documentId and then scroll."
  },
  {
    "id": "A-032",
    "pass": "A",
    "title": "J7.3/J7.4 — yesterday's run has no values left to walk: the preview cache TTL is 24 h, and the offered recovery starts a brand-new run against head",
    "severity": "blocker",
    "type": "design-gap",
    "evidence": "packages/graph-workflow/src/cache/constants.ts:11",
    "surfaces": ["preview-widget", "cache-evicted-alert", "wire-peek", "preview:query"],
    "disposition": "fix",
    "rationale": "`DEFAULT_CACHE_TTL_MS = 24 * 60 * 60 * 1000` and the GC sweep deletes expired rows. J7's premise — \"The run happened yesterday\" — puts every intermediate value at or past expiry, so replaying it yields `cache-evicted-alert` on node after node. The alert's only remedy is a Re-run button which POSTs `{initialCtx}` with no `workflowVersionId` (apps/frontend/src/features/workflow-builder/preview/CacheEvictedAlert.tsx:143), i.e. against the CURRENT head config — compounding A-027 — and, per A-001, cancelling anything else running on that lineage. It also violates J7 step 8's stated constraint (\"without … creating a second official result for it\"): the re-run is an ordinary run and appears in run history as one."
  },
  {
    "id": "A-033",
    "pass": "A",
    "title": "J7.5 — a step's INPUTS are never shown; only outputs are cached, so \"handed bad input\" vs \"corrupted good input\" must be inferred from the neighbour's preview",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/preview/preview.types.ts:23",
    "surfaces": ["preview-widget", "wire-peek", "settings-panel:inputs"],
    "disposition": "fix",
    "rationale": "`ActivityOutputPreview` is `{outputCtx, outputKind, createdAt, expiresAt}` and the cache decorator snapshots only the node's declared output leaf paths (`collectOutputLeafPaths` / `snapshotCtxDelta`, apps/temporal/src/graph-engine/node-executors.ts:223/:246). `wire-peek` partially rescues step 5 — clicking the incoming data wire reads the PRODUCER's `outputCtx` at that ctx key and works in replay (WirePeekPopover.tsx:79 uses `activeRunId` regardless of `isReplay`). But that only works where a derived data wire exists: a port bound to a plain declared ctx key, an input satisfied by `initialCtx`, or any control-flow node produces no wire and therefore no peek, and A-003 means the peeked value is a blob pointer for the whole OCR chain anyway."
  },
  {
    "id": "A-034",
    "pass": "A",
    "title": "J7.6 — which branch fired is not recorded or rendered: edge animation requires a node to be `running`, so in replay no edge is ever highlighted",
    "severity": "major",
    "type": "impl-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/run/active-edges.ts:34",
    "surfaces": ["run:active-edges", "canvas:wire", "canvas:edge-label", "run-state"],
    "disposition": "fix",
    "rationale": "`computeActiveEdges` skips any edge whose source is not currently `running`. Every node in a replayed run is terminal, so the returned set is always empty and the canvas shows no taken path. The engine does record the chosen branch (`state.selectedEdges`) but that is not exposed by the node-statuses query or anything else. Dana's step 6 (\"which section this page was assigned to, and whether the confidence check sent it to review\") is only inferable by comparing which downstream nodes have a status entry against which are absent — and `preview-widget`'s copy for both cases is the same sentence, \"This step didn't run in this run\" (PreviewWidget.tsx:64), which conflates \"branch not taken\" with \"run never got there\"."
  },
  {
    "id": "A-035",
    "pass": "A",
    "title": "J7.6 — a human correction leaves no trail Dana can read: audit rows exist server-side but no surface renders reviewer, time, or before/after values",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/hitl/hitl.service.ts:378",
    "surfaces": ["preview-widget", "run-history-drawer", "run-row"],
    "disposition": "defer",
    "rationale": "`submitCorrections` writes per-field rows (`original_value`, `corrected_value`, `original_conf`, `action`) and a `review_corrections_submitted` audit event, so the data Dana needs exists. Nothing in the frontend reads it — a grep of `apps/frontend/src/features` and `src/pages` for an audit viewer finds only a benchmarking component. J7's \"done\" requires \"if the cause was a human correction, she can name the reviewer and the session\". Deferring rather than fix-now: this is a viewer over data that already exists, and it is downstream of A-019/A-020 (today a human correction cannot influence a graph run's result at all)."
  },
  {
    "id": "A-036",
    "pass": "A",
    "title": "J7.7 — two runs cannot be compared; Compare-to-head diffs configs, never run values",
    "severity": "major",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/versioning/CompareToHeadModal.tsx:1",
    "surfaces": ["compare-to-head", "run-history-drawer", "preview-widget"],
    "disposition": "defer",
    "rationale": "The only comparison surface in the product compares a version's config against head. Replay is single-run: `RunStateContext` holds one `activeRunId`, so two runs cannot be on screen at once, let alone their per-node values. J7 step 7 (\"compare against a run of the same package that came out right, if one exists\") is a wall. Deferring — it is conditional in the journey (\"if one exists\") and is dominated by A-032, which usually means the good run's values are gone too."
  },
  {
    "id": "A-037",
    "pass": "A",
    "title": "J7.8 — no partial re-run: every re-execution is a whole-graph run from the entry node, and it becomes an official run in history",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/backend-services/src/workflow/dto/start-run.dto.ts:21",
    "surfaces": ["run-drawer", "cache-evicted-alert", "run-history-drawer"],
    "disposition": "defer",
    "rationale": "SPECULATIVE REQUIREMENT: the brief flags J7 step 8 as inferred. Confirmed as a wall — `StartRunRequestDto` takes `initialCtx` + `workflowVersionId` only; there is no start-node, node-subset, or dry-run field, and every run lands in run history with no marking. The activity-output cache does give a *partial* answer in practice (test-plan 9.6: unchanged upstream nodes come back `skipped` from cache, so only the suspect node and its downstream re-execute), but the run is still full-graph and still official. Ranking minor and deferring per the brief's instruction to rank speculative requirements lower."
  },
  {
    "id": "A-038",
    "pass": "A",
    "title": "Cross-journey — a node's problems badge counts unbound inputs but not any of the failure modes the journeys actually hit",
    "severity": "minor",
    "type": "design-gap",
    "evidence": "apps/frontend/src/features/workflow-builder/auto-wire-validation.ts:17",
    "surfaces": ["canvas:node-badge", "validation-drawer", "validation-engine", "topbar:validation-button"],
    "disposition": "defer",
    "rationale": "The unified problems surface merges core validator errors, auto-wire input health, and map-body reachability. Nothing in it flags: an output nothing consumes (A-005), unbounded map concurrency (A-016), a humanGate whose signal name nothing sends (A-023), a control-flow node with no output bindings (A-022), or a node with no error policy inside a fan-out (A-010/A-018). Every one of those is a state a journey author lands in while the top bar reads \"Valid\". Recording once at low severity rather than as five separate findings; the individual fixes are the ones ranked above."
  }
]
