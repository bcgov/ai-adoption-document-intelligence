# Pin exact versions on install (no ^ or ~ prefixes)
save-exact=true
# Disable lifecycle scripts by default (postinstall, preinstall, etc.)
ignore-scripts=true
# Minimum release age in days — blocks packages published less than 3 days ago
min-release-age=3
